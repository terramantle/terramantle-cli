//! Typed API client for the Terramantle registry/worker (SPEC §7).
//!
//! Placeholder for a later slice (§10 slice 3): the concrete `ureq`-backed
//! methods (one fn per endpoint) and error→exit-code mapping land there. For
//! now this crate carries only shared model stubs so downstream crates can
//! depend on it.

pub mod types;
